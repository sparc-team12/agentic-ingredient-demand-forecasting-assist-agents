"""ASGI middleware modules (e.g. session/auth cookie handling, CORS, logging).

Responsibility: cross-cutting request/response concerns applied in `main.py`.
Empty placeholder — no middleware registered yet; the per-user session-cookie
mechanism (per DEC-005) is implemented here in a later development stage.
"""
