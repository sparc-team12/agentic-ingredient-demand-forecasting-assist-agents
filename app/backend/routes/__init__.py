"""API route modules (FastAPI `APIRouter`s), one module per resource/feature.

Responsibility: HTTP-layer request/response handling only — delegate business
logic to `services/`. Empty placeholder; the health-check route currently
lives directly in `main.py` and will move here once a second route exists.
"""
