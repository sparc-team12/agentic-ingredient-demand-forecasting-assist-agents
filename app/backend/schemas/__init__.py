"""Pydantic request/response schemas (HTTP contract layer).

Responsibility: define and validate the shape of data crossing the HTTP
boundary. Business logic and persistence stay in `services`/`db`.
"""
