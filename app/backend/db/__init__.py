"""Persistence layer placeholder.

Responsibility: own the SQLAlchemy engine/session setup for the SQLite
database (per DEC-005: SQLAlchemy ORM over SQLite). No ORM models or a
production schema are defined here yet — that belongs to a later,
requirements-driven development stage, not the scaffold.
"""
