# Scaffold-only environment contract. Copy to .env.local for local development.
# Never commit a real .env file.

# Base URL of the FastAPI backend, consumed via import.meta.env.VITE_API_URL.
VITE_API_URL=http://localhost:8000
